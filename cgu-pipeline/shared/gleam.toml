name = "shared"
version = "0.1.0"
target = "erlang"

description = "Shared transport and protocol types for master and worker."

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
gleam_json = ">= 3.0.2 and < 4.0.0"

[dev_dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
