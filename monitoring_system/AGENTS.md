# AGENTS.md

Operational deployment notes live in [deploy/RUNBOOK.md](deploy/RUNBOOK.md).

Use `task` (`go-task` in arch-based)

`panic` and `assert` are only acceptable in test files and server/src/(server.gleam,startup.gleam)

read about the packages in .knowledge/ instead of guessing how they work
